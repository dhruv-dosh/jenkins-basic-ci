version: '3.8'

services:
  web:
    build: ./portfolio-site
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - ./portfolio-site:/app
    restart: unless-stopped